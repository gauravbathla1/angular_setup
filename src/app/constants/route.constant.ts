export const LOGIN_ROUTE = {
  path: 'login',
  get url(): string {
    return `/${this.path}`;
  }
};

export const DASHBOARD_ROUTE = {
  path: 'dashboard',
  get url(): string {
    return `/${this.path}`;
  }
};





export const USER_ROUTE = {
  path: 'user',
  get url(): string {
    return `/${this.path}`
  }
}
export const INDIVIDUAL_USER_ROUTE = {
  path: 'individualuser',
  get url(): string {
    return `/${this.path}`
  }
}
export const ITEM_MANAGEMENT_ROUTE={
  path: 'item',
  get url(): string {
    return `/${this.path}`
  }
}

export const CHANGE_PASSWORD_ROUTE = {
  path: 'change-password',
  get url(): string {
    return `/${this.path}`
  }
}
export const FORGOT_PASSWORD_ROUTE = {
  path: 'forgot-password',
  get url(): string {
    return `/${this.path}`
  }
}
export const RESET_PASSWORD_ROUTE = {
  path: 'reset-password',
  get url(): string {
    return `/${this.path}`
  }
}
















